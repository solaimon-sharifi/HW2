# Copy this file to `.env` and fill in real values. Do NOT commit the real `.env` file.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=REPLACE_ME
POSTGRES_DB=fastapi_db
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=REPLACE_ME
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=REPLACE_ME
OPENAI_API_KEY=REPLACE_ME

# For local testing only: keep secrets out of source control and use GitHub Actions Secrets for CI.
